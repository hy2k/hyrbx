Got tired of copy-pasting some utils from one game to another, so I extracted it into a package.

Scoped at `@hyrbx/*` so that I won't possibly bloat the `@rbxts/*` namespace with my own dumb packages.
